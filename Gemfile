source "https://rubygems.org"

# Use standard Jekyll instead of the bulky github-pages meta-gem
gem "jekyll", "~> 4.3"

# Modern Ruby versions require this explicitly to run the local server
gem "webrick", "~> 1.8"

# If your blog uses a specific theme (like Minima), uncomment the line below:
# gem "minima", "~> 2.5"
